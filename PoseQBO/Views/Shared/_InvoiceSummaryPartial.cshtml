@model Invoice

<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">
        <h4>
            @Model.CustomerRef.name
            <span class="badge badge-pill badge-primary">
                <small>@Model.Line.FirstOrDefault().Amount</small>
            </span>
        </h4>
    </div>
</div>